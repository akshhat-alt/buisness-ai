# Business AI — Python packages
#
# Install with: pip install -r requirements.txt
#
# Deliberately minimal: no lxml/bs4 (stdlib html.parser is enough for
# business-site text extraction), no boto3/redis (single-node for MVP),
# no yt-dlp/whisper/ebooklib (out of scope for v1 — see ARCHITECTURE.md).

pydantic>=2,<3
chromadb>=0.5,<2
openai>=1,<2
pypdf>=6.16,<7
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-multipart>=0.0.9,<1
python-dotenv>=1,<2
pyjwt>=2.8.0,<3
cryptography>=50,<51
